$jsonFilePath = '.\order.json'; $targetIP = '127.0.0.1'; $targetPort = 24680; $client = New-Object System.Net.Sockets.UdpClient; $fileContent = Get-Content -Path $jsonFilePath -Raw; $bytesToSend = [System.Text.Encoding]::UTF8.GetBytes($fileContent); $client.Send($bytesToSend, $bytesToSend.Length, $targetIP, $targetPort); $client.Close(); Write-Host "Sent content of '$jsonFilePath' via UDP to $targetIP`:$targetPort"